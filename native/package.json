{
  "name": "ratarmount-native",
  "version": "0.1.0",
  "private": true,
  "description": "napi build --features napi-addon adds to default features (includes session). Do not pass --no-default-features.",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "ratarmount-native",
    "dtsCache": false
  },
  "scripts": {
    "build": "napi build --platform --esm --features napi-addon",
    "build:debug": "napi build --platform --esm --features napi-addon"
  },
  "devDependencies": {
    "@napi-rs/cli": "3.4.1"
  }
}
