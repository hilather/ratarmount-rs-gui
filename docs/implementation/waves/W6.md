# W6 — OS integration

**Owner:** packaging / desktop agent  
**Depends on:** W4  
**Done when:** `ratarmount-gui archive.tar` opens that archive; Linux .desktop + MIME installed from `integrations/`; Extract here works.  
**PR:** PR 7

## Tasks

- [x] argv parser: open paths, `--extract-here`, `--extract-to [DIR] <archive>`, `--index-only`, `--silent`
- [x] `--extract-to` with dir omitted (or `--extract-to -- <archive>`) opens the folder picker; **never** treat the archive path as `destDir`
- [x] `--silent` maps config `overwrite = ask` → native `skip` (must not hang)
- [x] Linux: `.desktop` + MIME xml as in [../../architecture/04-os-integration.md](../../architecture/04-os-integration.md)
- [x] Linux desktop actions: Extract here
- [x] macOS Info.plist document types
- [x] Windows registry fragment (even if installer is later). ExtractTo: `ratarmount-gui.exe --extract-to -- "%1"`
- [x] Settings: register / unregister associations (best-effort per OS)
- [x] Unsafe-path toggle default off
- [x] **Create** `docs/qa-os-integration.md` in this PR (does not exist yet) and fill the manual checklist
- [x] Argv unit test: ExtractTo / `--extract-to -- archive.tar` does not interpret the archive as `destDir`

## Out of scope

Signed/notarized installers (W7). Do not steal `inode/directory`. Do not register `.exe` / `.msi`.
