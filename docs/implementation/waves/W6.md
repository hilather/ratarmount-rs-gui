# W6 — OS integration

**Owner:** packaging / desktop agent  
**Depends on:** W4  
**Done when:** `ratarmount-gui archive.tar` opens that archive; Linux .desktop + MIME installed from `integrations/`; Extract here works.  
**PR:** PR 7

## Tasks

- [ ] argv parser: open paths, `--extract-here`, `--extract-to [DIR] <archive>`, `--index-only`, `--silent`
- [ ] `--extract-to` with dir omitted (or `--extract-to -- <archive>`) opens the folder picker; **never** treat the archive path as `destDir`
- [ ] `--silent` maps config `overwrite = ask` → native `skip` (must not hang)
- [ ] Linux: `.desktop` + MIME xml as in [../../architecture/04-os-integration.md](../../architecture/04-os-integration.md)
- [ ] Linux desktop actions: Extract here
- [ ] macOS Info.plist document types
- [ ] Windows registry fragment (even if installer is later). ExtractTo: `ratarmount-gui.exe --extract-to -- "%1"`
- [ ] Settings: register / unregister associations (best-effort per OS)
- [ ] Unsafe-path toggle default off
- [ ] **Create** `docs/qa-os-integration.md` in this PR (does not exist yet) and fill the manual checklist
- [ ] Argv unit test: ExtractTo / `--extract-to -- archive.tar` does not interpret the archive as `destDir`

## Out of scope

Signed/notarized installers (W7). Do not steal `inode/directory`. Do not register `.exe` / `.msi`.
