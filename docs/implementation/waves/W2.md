# W2 — Wire ratarmount-session

**Owner:** engine-integration agent  
**Depends on:** W1, engine **G0 + G1 + G2**, plus **G5.1** (`Session: Send`) and **G5.3** (default features do not pull fuse/nfs/smb/http)  
**Done when:** opening a real fixture TAR builds/reuses a sidecar and lists real members. No member bytes in JS.  
**PR:** PR 10 (session 0.1.30 pin + open/list/lookup/close/index)

## Tasks

- [x] Cargo dep on `ratarmount-session` 0.1.30 (git tag `v0.1.30`). **Do not import the `ratarmount` binary crate.**
- [x] `default-features = false` plus an empty extra allowlist; never pull fuse/nfs/smb/http-export on the explorer path
- [x] Replace fake `open`/`list`/`lookup`/`close` with Session (fake stays behind `RGUI_FAKE=1` / `for_test()`)
- [x] Consume engine `resolve_index` as a **post-success log only** — do not reimplement `local-index-v1` naming; do not preflight as an open gate
- [x] `open` with `if-invalid` / `always` always returns `{ jobId }`; `Session::open_with_job` + `IndexBuildHooks` (not engine `IndexJob::run`)
- [x] Cancel token hooked to `cancel(jobId)` — worker maps `Error::Cancelled` to a no-op when the job is already cancelled
- [x] Feature flags: do not link fuse/nfs
- [x] Test: 1k-member TAR fixture, page size 50, two pages, extract one file to a temp dir from Rust (`EngineSession::extract_to`)
- [x] Log line (debug only) of resolved index path — used by W5 UI (`rgui: resolved index path: …`). `RGUI_DEBUG=1` also eprints it

## Gate

Engine crate exists at tag `v0.1.30`. Native feature `session` is default-on. Production `open(never)` on a sidecar-less TAR is `NotFound`. `if-invalid` / `always` return `{ jobId }` then `jobSucceeded` with `JobState.session_id` set.
