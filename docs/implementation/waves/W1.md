# W1 — Native napi stubs

**Owner:** native agent  
**Depends on:** W0  
**Done when:** UI can call `pickFile` + `list` against a **fake** in-memory catalog.  
**PR:** PR 2

## Tasks

- [x] napi-rs module compiled from `native/`
- [x] Implement handle table + job table
- [x] Implement commands from [../../architecture/05-napi-contract.md](../../architecture/05-napi-contract.md) as stubs, including the **types** (`DirPage`, `DirEnt`, `FindPage`, `Config`, opaque `cursor`)
  - `pickFile` / `pickDir` — real OS dialogs
  - `open` — if source is the fixture path, return session 1; else error; allow `policy: 'memory'` in tests only
  - `list` — return paged fake dirents with `nextCursor: string | null` (not a raw offset)
  - `extract` overwrite `'skip' | 'replace'` only; reject `'ask'`
  - `extractPlan` stub: `files`/`bytes`/`conflictCount`; `conflicts` sample ≤ 50 + `conflictsTruncated`
  - `close` / `getConfig` / `setConfig` (in-memory)
- [x] Events: emit a dummy `indexProgress` then `jobSucceeded`; `jobFailed` includes `retryable`
- [x] `native --self-test` or `cargo test -p native`
- [x] Document how app.tsx imports the addon

## Addon import (app/)

After building the napi addon (`cd native && bun install && bun run build`, which enables the `napi-addon` Cargo feature), W3 loads it through `loadNativeAddon()` in `app/native-addon.ts`. That function dynamically `import`s `../native` so a missing `.node` does not fail process start; Open then fails (and can retry) with the “not built” error. W1 only documented the module path.

`RGUI_FAKE=1` lets `open` accept any path and serve the fake catalog (needed so W3 can use the file picker). `policy: 'memory'` is rejected unless `RGUI_FAKE=1`, `NativeApp::for_test()`, or `native --self-test`. `NativeApp::new()` during `cargo test` still rejects `memory` unless `RGUI_FAKE=1`.

W1 addon commands are synchronous (see [05-napi-contract.md](../../architecture/05-napi-contract.md)). `DirPage.nextCursor` / `totalHint` / `DirEnt.mtime` are JS `null` when absent (not a missing property).

## Out of scope

Linking ratarmount crates (that is W2). Do not add `readAll`.
