# W3 — Explorer chrome

**Owner:** UI agent  
**Depends on:** W1 (fake catalog is enough)  
**Done when:** a user can open a file picker, see breadcrumbs + virtualized rows, enter a directory, go up.  
**PR:** PR 3 (parallel with W2 / PR 4 after W1)

## Tasks

- [ ] Menu/toolbar: Open, Close
- [ ] Status bar: archive path, entry count hint, index path (shortened)
- [ ] Breadcrumb path
- [ ] `<virtual-list>` of current page; request next page near end
- [ ] Columns: name, size, mtime
- [ ] Keyboard: Enter / Backspace / j-k or arrows
- [ ] Empty / loading / error states
- [ ] testIds for GPUIX automation: `open`, `list`, `crumb-*`

## Out of scope

Extract, preview pane, settings, search box. Do not load all paths into React state.
