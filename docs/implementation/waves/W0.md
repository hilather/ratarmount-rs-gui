# W0 — Repository scaffold

**Owner:** tooling agent  
**Depends on:** nothing (documentation seed already landed)  
**Done when:** `bun run dev` opens an empty GPUIX window titled “ratarmount”.  
**PR:** PR 1 in [../../design/design.md](../../design/design.md) (must **not** rewrite architecture docs).

## Already done (documentation seed)

- [x] Repository exists with LICENSE (MIT, this project, 2026)
- [x] `docs/` tree (architecture, design, implementation, engine snapshot, ADRs)
- [x] `AGENTS.md`, `CONTRIBUTING.md`, `START-HERE.md`, `.gitignore`
- [x] README landing page with hard rules and pointers

## Tasks (remaining W0 code/tooling)

- [x] `app/` from `bunx @gpuix/cli new` (or equivalent current GPUIX template)
- [x] Window: 1100×720, dark background, placeholder text “Open an archive”
- [x] `native/` empty crate with `cdylib` + `rlib`, not wired yet
- [x] README: how to run (fill the W0 section), pointer to docs, hard rules (do not dump wave boards into README)
- [x] CI skeleton: `bun run typecheck` then `bun test` / `cargo check -p native` / `cargo test -p native`. Typecheck must run first; leftover `<<<<<<<` markers fail the app job.
- [x] Trivial automated test: `native_crate_links` (or equivalent `#[test]` in `native/`) so W0 is not “manual only.” Window-title smoke remains **manual** (AGENTS.md W0 waiver).

## Out of scope

Engine session API, file dialogs, packaging. Do not rewrite `docs/architecture/` or `docs/design/design.md` in this wave.
