# W8 — Polish

**Owner:** mixed  
**Depends on:** W5, W6  
**Done when:** search works; optional FUSE/HTTP buttons hide when CLI/session feature missing; list stays smooth at 100k fixture rows.  
**PR:** PR 9

## Tasks

- [x] Search box → `find` paged (engine G3 consumed: production `Session::find` with opaque `f1:` cursors; search box default is glob so the first keystroke is one SQL page; `fts:` / `mode: 'fts'` is opt-in, not a side effect of `open`; fake catalog stays for UI/100k tests; no dump of 2M hits)
- [x] Reveal as folder (Unix) + unmount (`probeFeatures().fuse`; hidden when CLI/fuse runtime missing)
- [x] Share via HTTP + copy URL (`probeFeatures().http`; hidden when CLI/session feature missing)
- [x] Recent archives list (paths only, in config)
- [x] Drag-and-drop archive onto window (Linux **X11** `startFileDropWatch` / `fileDrop`; GPUIX 0.6 has no React `onDrop`. Wayland/macOS/Windows: not watched in v1)
- [x] 100k-member fixture perf check (scroll, no JS array of 100k)
- [x] Accessibility: focus ring, keyboard-only extract
- [x] Crash log location documented
- [x] README screenshots / short usage

## Still later (not W8)

Write overlay UI, Windows FUSE, browser target, auto-update.
