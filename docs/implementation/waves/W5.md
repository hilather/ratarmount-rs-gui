# W5 — Settings and index policy

**Owner:** **one agent** covering UI + native (default). Split only with an explicit file split in the spawn prompt.  
**Depends on:** W2, **W3** (settings chrome hangs off explorer `app/`), engine G4  
**Done when:** config.toml round-trips and sibling-not-writable offers cache.  
**PR:** PR 6 (depends PR 3 and PR 4)

## Tasks

- [x] Load/save config at the paths in [../../architecture/02-index-storage.md](../../architecture/02-index-storage.md)
- [x] Settings screen: policy, recreate, preview cap, extra index dirs, cache cap. Hide `memory`. Do not persist `memory`.
- [x] On `SiblingNotWritable`: dialog “Use user cache?” + remember-volume checkbox
- [x] “Clear local index cache” button (only `local-index-v1`, never the legacy `$XDG_CACHE_HOME/ratarmount/` parent or sibling files)
- [x] Show resolved index location in status bar with policy badge (hint only until G4; user-cache shows `user cache`, not a sha256 key)
- [x] Temp policy warning copy
- [ ] Consume engine G4 `resolve_index` — **blocked.** `TODO(engine)` on `session::resolve_index`. Native probes sibling writability and returns `SiblingNotWritable`; it does **not** reimplement the seven-step table or `local-index-v1` naming.
- [x] Tests: write config, reopen process, same policy; **config `preview.max_bytes = 65 MiB` still clamps to 64 MiB**; cache-clear does not delete sibling files

## Out of scope

File associations (W6). Native must clamp preview cap to 64 MiB. `/tmp` is not the default policy. Password dialog is W4.

## Engine G4

As of this wave, `resolve_index` / `SiblingNotWritable` are **not** in the engine session crate. GUI config.toml, clamp, settings UI, and dialog plumbing land anyway. Index discovery stays with the engine; see `native/src/session.rs`.
