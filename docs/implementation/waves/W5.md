# W5 — Settings and index policy

**Owner:** **one agent** covering UI + native (default). Split only with an explicit file split in the spawn prompt.  
**Depends on:** W2, **W3** (settings chrome hangs off explorer `app/`), engine G4  
**Done when:** config.toml round-trips and sibling-not-writable offers cache.  
**PR:** PR 6 (depends PR 3 and PR 4)

## Tasks

- [ ] Load/save config at the paths in [../../architecture/02-index-storage.md](../../architecture/02-index-storage.md)
- [ ] Settings screen: policy, recreate, preview cap, extra index dirs, cache cap. Hide `memory`. Do not persist `memory`.
- [ ] On `SiblingNotWritable`: dialog “Use user cache?” + remember-volume checkbox
- [ ] “Clear local index cache” button (only `local-index-v1`, never the legacy `$XDG_CACHE_HOME/ratarmount/` parent or sibling files)
- [ ] Show resolved index location in status bar with policy badge
- [ ] Temp policy warning copy
- [ ] Consume engine G4 `resolve_index` — do not reimplement the seven-step table in GUI
- [ ] Tests: write config, reopen process, same policy; **config `preview.max_bytes = 65 MiB` still clamps to 64 MiB**

## Out of scope

File associations (W6). Native must clamp preview cap to 64 MiB. `/tmp` is not the default policy. Password dialog is W4.
